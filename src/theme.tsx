import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
   palette: {
    mode: "dark",
    primary: {
      main: '#8e0000',
    },
    secondary: {
      main: '#edf2ff',
    },
  },
});

export default theme;